[build]
  base    = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "18"

# SPA: all routes fall back to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
